---
import Section from "./ui/Section.astro";

const services = [
    {
        title: "Regular Maintenance",
        image: "/images/services/regular-maintenance.png",
        popular: true,
    },
    {
        title: "Deep Cleaning",
        image: "/images/services/deep-cleaning.png",
        popular: true,
    },
    {
        title: "Move-In / Move-Out",
        image: "/images/services/move-in-move-out.png",
    },
    {
        title: "Airbnb Turnover",
        image: "/images/services/airbnb-rentals.png",
    },
    {
        title: "Post-Construction",
        image: "/images/services/post-construction.png",
    },
    {
        title: "Office Cleaning",
        image: "https://images.unsplash.com/photo-1497366216548-37526070297c?auto=format&fit=crop&q=80&w=1000",
    },
];

const allServices = [...services, ...services];
---

<Section id="services" class="bg-background-mint overflow-hidden py-16 md:py-24" fullWidth>
    <div class="text-center max-w-2xl mx-auto mb-16 px-4 animate-fade-in-up">
        <span
            class="inline-flex items-center gap-1.5 text-text-600 font-semibold tracking-wider uppercase text-xs bg-white border border-border-light px-4 py-1.5 rounded-full mb-4"
            >What are you looking for?</span
        >
        <h2
            class="text-4xl md:text-5xl font-bold text-secondary mb-6 font-sans tracking-tight"
        >
            Our Services
        </h2>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-x-4 gap-y-8 px-4 lg:hidden">
        {
            services.map((service, index) => (
                <div
                    class="flex flex-col items-center group/card"
                    style={`animation: fade-in-up 0.6s ease-out forwards ${index * 100}ms; opacity: 0;`}
                >
                    <div class="w-full aspect-[3/4] rounded-2xl overflow-hidden shadow-md relative">
                        <img
                            src={service.image}
                            alt={service.title}
                            class="w-full h-full object-cover"
                            loading="lazy"
                        />
                        <div class="absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent" />
                        {service.popular && (
                            <span class="absolute top-3 right-3 bg-primary text-white text-[10px] font-bold uppercase tracking-wider px-2.5 py-1 rounded-full shadow-lg">
                                Popular
                            </span>
                        )}
                    </div>

                    <h3 class="mt-4 text-lg font-bold text-secondary text-center tracking-tight leading-tight">
                        {service.title}
                    </h3>
                </div>
            ))
        }
    </div>

    <div class="hidden lg:block relative w-full overflow-hidden group">
        <div class="flex w-max animate-scroll py-4">
            {
                allServices.map((service) => (
                    <div class="flex-none px-3">
                        <div class="w-[15vw] flex flex-col items-center group/card">
                            <div class="w-full aspect-[3/5] rounded-[2rem] overflow-hidden shadow-lg relative">
                                <img
                                    src={service.image}
                                    alt={service.title}
                                    class="w-full h-full object-cover"
                                />
                                <div class="absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent" />
                                {service.popular && (
                                    <span class="absolute top-4 right-4 bg-primary text-white text-xs font-bold uppercase tracking-wider px-3 py-1.5 rounded-full shadow-lg">
                                        Popular
                                    </span>
                                )}
                            </div>

                            <h3 class="mt-5 text-xl font-bold text-secondary text-center tracking-tight">
                                {service.title}
                            </h3>
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</Section>

<style>
    @keyframes scroll {
        0% {
            transform: translateX(0);
        }
        100% {
            transform: translateX(-50%);
        }
    }

    .animate-scroll {
        animation: scroll 50s linear infinite;
    }

    @media (max-width: 768px) {
        .animate-scroll {
            animation-duration: 30s;
        }
    }
</style>
